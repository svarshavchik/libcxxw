<!--

Copyright 2017-2019 Double Precision, Inc.
See COPYING for distribution information.

XML-based definition of a template-driven object initialization,
used mostly to generate predefined grid layouts.

Transformed by the "parseuicompiler.xsl" stylesheet.

The top level element is <api>.

<api> contains a list of <parser>s.

Each <parser>'s <name> robo-generates a

uicompiler::<name>_parser()

[objectname]_parser()'s first parameter is

const theme_parser_lock &lock

Each <parser> element contains a list of <parameter> elements.
[objectname]_parser() returns a
functionref<void (const [parameter]/[type] &)>, using each <parameter>'s
<type>. An @mutable attribute leaves off the "const" part.


Additionally, a parseconfig wrapper also gets robogenerated:

vector<functionref< ...>>
 <name>_parseconfig(const theme_parser_lock &lock)
{
    auto config=vector<functionref< ... >>::create();

    config->reserve(lock->get_child_element_count());

    if (lock->get_first_element_child())
        do
        {
            config.push_back([objectname]_parser(lock));
        } while (lock->get_next_element_sibling());
}

The parseconfig lock's child elements, and for each
child element, a new value is appended to the vector that's passed as the
second argument.

The contents of <name>_parser() gets generated from the <parser>'s
<function> elements.

The <name>_parser() finds a matching <function> for the current element
as follows:

1. Each <function>'s <name>:

auto name=lock->name();  // lock is the original lock's child element.

if (name == "<function>'s <name>"

Then, if the <function> has any <condition>'s:

    &&

If <condition> has an <exists>:

       single_value_exists(lock, "<exist>'s contents")

Otherwise

       lowercase_single_value(lock, "<condition>'s <name>")
                             == "<condition>'s <value>"

    )
{
     // Parameter code

     return [=]
         ( // List of top level parameters)
         {

            <object>-><invoke>( // Forward list
                                       // List of values
                                     );
         });
}

If the <function>'s <parameter> has a <scalar>, instead of the auto value,
which was not declared, the substitute text in <scalar>

2. If none of the <function>s match, an exception is thrown. The order
of <function>s is the order of the attempted matches.


For each matched function:

Parameter code:

For each one of the <function>'s <parameter>s, generates:

auto <name>_value=<type>(lock, "<name>", "<function>'s <name>");

Does not generate anything if <parameter> has a <scalar>.

List of top level's parameters:

For each <parser>'s <parameter>:
      const <type> &<name>

Forward list:

For each <parser>'s <forward>:
      <forward>'s text

List of values:

      The list of the auto values that were declared.

-->

<api>
  <parser>
    <name>gridlayout</name>

    <parameter>
      <type>gridlayoutmanager</type>
      <name>layout</name>
    </parameter>

    <parameter mutable="1">
      <type>uielements</type>
      <name>elements</name>
    </parameter>

    <!-- grid_halign -->

    <function>
      <name>halign</name>
      <invoke>col_alignment</invoke>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>halign</name>
	<type>to_halign_value</type>
      </parameter>
    </function>

    <!-- grid_valign -->

    <function>
      <name>valign</name>
      <invoke>row_alignment</invoke>

      <object>layout</object>

      <parameter>
	<name>row</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>valign</name>
	<type>to_valign_value</type>
      </parameter>
    </function>

    <!-- padding -->

    <function>
      <name>padding</name>
      <invoke>col_left_padding</invoke>

      <condition>
	<name>side</name>
	<value>left</value>
      </condition>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>col_right_padding</invoke>

      <condition>
	<name>side</name>
	<value>right</value>
      </condition>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>row_top_padding</invoke>

      <condition>
	<name>side</name>
	<value>top</value>
      </condition>

      <object>layout</object>

      <parameter>
	<name>row</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>row_bottom_padding</invoke>

      <condition>
	<name>side</name>
	<value>bottom</value>
      </condition>

      <object>layout</object>

      <parameter>
	<name>row</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <!-- border -->

    <function>
      <name>default_col_border</name>
      <invoke>default_col_border</invoke>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <function>
      <name>default_row_border</name>
      <invoke>default_row_border</invoke>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <!-- percent -->

    <function>
      <name>percent</name>
      <invoke>requested_col_width</invoke>

      <condition>
	<exists>col</exists>
      </condition>

      <object>layout</object>

      <parameter>
	<name>col</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>percentage</name>
	<type>to_percentage_t</type>
      </parameter>
    </function>

    <function>
      <name>percent</name>
      <invoke>requested_row_height</invoke>

      <condition>
	<exists>row</exists>
      </condition>

      <object>layout</object>

      <parameter>
	<name>row</name>
	<type>to_size_t</type>
      </parameter>

      <parameter>
	<name>percentage</name>
	<type>to_percentage_t</type>
      </parameter>
    </function>

    <function>
      <name>append_row</name>
      <invoke>gridlayout_append_row</invoke>

      <parameter>
	<scalar>layout</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<name>name</name>
	<type>lookup_gridfactory_generators</type>
      </parameter>
    </function>

    <!-- background_color -->

    <function>
      <name>background_color</name>
      <object>layout</object>
      <invoke>set_background_color</invoke>

      <parameter>
	<name>color</name>
	<type>single_value</type>
	<lookup>generators.lookup_color</lookup>
      </parameter>
    </function>

    <!-- remove_background_color -->

    <function>
      <name>remove_background_color</name>
      <object>layout</object>
      <invoke>remove_background_color</invoke>
    </function>
  </parser>

  <parser>
    <name>gridfactory</name>

    <parameter>
      <type>gridfactory</type>
      <name>factory</name>
    </parameter>

    <parameter mutable="1">
      <type>uielements</type>
      <name>elements</name>
    </parameter>

    <!-- halign -->

    <function>
      <name>halign</name>
      <invoke>halign</invoke>

      <object>factory</object>

      <parameter>
	<name>halign</name>
	<type>to_halign_value</type>
      </parameter>
    </function>

    <!-- valign -->

    <function>
      <name>valign</name>
      <invoke>valign</invoke>

      <object>factory</object>

      <parameter>
	<name>valign</name>
	<type>to_valign_value</type>
      </parameter>
    </function>

    <!-- padding -->

    <function>
      <name>padding</name>
      <invoke>padding</invoke>

      <condition>
	<name>side</name>
	<value>all</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>left_padding</invoke>

      <condition>
	<name>side</name>
	<value>left</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>right_padding</invoke>

      <condition>
	<name>side</name>
	<value>right</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>top_padding</invoke>

      <condition>
	<name>side</name>
	<value>top</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <function>
      <name>padding</name>
      <invoke>bottom_padding</invoke>

      <condition>
	<name>side</name>
	<value>bottom</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>padding</name>
	<type>single_value</type>
	<lookup>generators.lookup_dim</lookup>
      </parameter>
    </function>

    <!-- colspan -->

    <function>
      <name>colspan</name>
      <invoke>colspan</invoke>
      <object>factory</object>

      <parameter>
	<name>columns</name>
	<type>to_size_t</type>
      </parameter>
    </function>

    <!-- rowspan -->

    <function>
      <name>rowspan</name>
      <invoke>rowspan</invoke>
      <object>factory</object>

      <parameter>
	<name>rows</name>
	<type>to_size_t</type>
      </parameter>
    </function>

    <!-- border -->

    <function>
      <name>border</name>
      <invoke>border</invoke>

      <condition>
	<name>side</name>
	<value>all</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <function>
      <name>border</name>
      <invoke>left_border</invoke>

      <condition>
	<name>side</name>
	<value>left</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <function>
      <name>border</name>
      <invoke>right_border</invoke>

      <condition>
	<name>side</name>
	<value>right</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <function>
      <name>border</name>
      <invoke>top_border</invoke>

      <condition>
	<name>side</name>
	<value>top</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <function>
      <name>border</name>
      <invoke>bottom_border</invoke>

      <condition>
	<name>side</name>
	<value>bottom</value>
      </condition>

      <object>factory</object>

      <parameter>
	<name>border</name>
	<type>single_value</type>
	<lookup>generators.lookup_border</lookup>
      </parameter>
    </function>

    <!-- element -->

    <function>
      <name>element</name>

      <invoke>generate</invoke>

      <parameter>
	<scalar>factory</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<type>single_value</type>
	<name>name</name>
      </parameter>
    </function>

    <!-- book -->
    <function>
      <name>book</name>

      <invoke>container_booklayoutmanager</invoke>

      <parameter>
	<scalar>factory</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<name>name</name>
	<type>lookup_booklayoutmanager_generators</type>
      </parameter>

      <parameter>
	<type>optional_value</type>
	<name>background_color</name>
      </parameter>

      <parameter>
	<type>optional_value</type>
	<name>border</name>
      </parameter>
    </function>

    <!-- grid -->

    <function>
      <name>grid</name>
      <invoke>container_gridlayoutmanager</invoke>

      <parameter>
	<scalar>factory</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<type>lookup_gridlayoutmanager_generators</type>
	<name>name</name>
      </parameter>

      <parameter>
	<type>optional_value</type>
	<name>background_color</name>
      </parameter>
    </function>
  </parser>

  <!-- book layout manager parser -->

  <parser>
    <name>booklayout</name>

    <parameter>
      <type>booklayoutmanager</type>
      <name>layout</name>
    </parameter>

    <parameter mutable="1">
      <type>uielements</type>
      <name>elements</name>
    </parameter>

    <function>
      <name>append_pages</name>
      <invoke>booklayout_append_pages</invoke>

      <parameter>
	<scalar>layout</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<name>name</name>
	<type>lookup_bookpagefactory_generators</type>
      </parameter>
    </function>

    <function>
      <name>open</name>
      <invoke>open</invoke>
      <object>layout</object>

      <parameter>
	<name>page</name>
	<type>to_size_t</type>
      </parameter>
    </function>
  </parser>

  <!-- book page factory -->

  <parser>
    <name>bookpagefactory</name>

    <parameter>
      <type>bookpagefactory</type>
      <name>factory</name>
    </parameter>

    <parameter mutable="1">
      <type>uielements</type>
      <name>elements</name>
    </parameter>

    <!-- halign -->

    <function>
      <name>halign</name>
      <invoke>halign</invoke>

      <object>factory</object>

      <parameter>
	<name>halign</name>
	<type>to_halign_value</type>
      </parameter>
    </function>

    <!-- valign -->

    <function>
      <name>valign</name>
      <invoke>valign</invoke>

      <object>factory</object>

      <parameter>
	<name>valign</name>
	<type>to_valign_value</type>
      </parameter>
    </function>

    <function>
      <name>add</name>
      <invoke>container_addbookpage</invoke>

      <parameter>
	<scalar>factory</scalar>
      </parameter>

      <parameter>
	<scalar>elements</scalar>
      </parameter>

      <parameter>
	<name>label</name>
	<type>single_value</type>
      </parameter>

      <parameter>
	<name>shortcut</name>
	<type>optional_value</type>
      </parameter>

      <parameter>
	<name>layout</name>
	<type>lookup_gridlayoutmanager_generators</type>
      </parameter>
    </function>
  </parser>
</api>
